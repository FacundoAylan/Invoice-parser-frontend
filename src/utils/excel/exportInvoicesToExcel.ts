import type { InvoiceList } from "@/types/invoice";

const ExportInvoicesToExcel = (invoices: InvoiceList[]) => {
  //Aca iria mi codigo sin tal solo tuviera uno
  console.log(invoices);
};

export default ExportInvoicesToExcel;
